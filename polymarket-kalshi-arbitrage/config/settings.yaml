scanner:
  # Use "simulation" for demos and tests. Use "live" to call public exchange APIs.
  data_mode: "live"
  autostart: true
  interval_seconds: 180
  # Use 0 to scan all open markets returned by pagination.
  polymarket_limit: 1000
  kalshi_limit: 1000
  polymarket_page_size: 500
  kalshi_page_size: 1000
  max_pages_per_venue: 10
  max_concurrent_requests: 4
  retry_attempts: 2
  retry_backoff_seconds: 0.5

  # Fetching live Polymarket books is slower because it calls one CLOB book per token.
  include_polymarket_orderbooks: false

  # Matching and signal thresholds.
  min_match_confidence: 0.58
  min_net_edge: 0.01
  signal_limit: 50

  # Risk/dry-run sizing.
  max_position_size: 25.0
  unknown_liquidity_size: 5.0

  # Configure these after confirming venue fee schedules for the account.
  polymarket_fee_bps: 0.0
  kalshi_fee_bps: 0.0

api:
  polymarket_gamma_base: "https://gamma-api.polymarket.com"
  polymarket_clob_base: "https://clob.polymarket.com"
  kalshi_base: "https://external-api.kalshi.com/trade-api/v2"
  request_timeout_seconds: 12.0
  user_agent: "polymarket-kalshi-arbitrage/0.1"

manual_pairs_path: "config/manual_pairs.yaml"
