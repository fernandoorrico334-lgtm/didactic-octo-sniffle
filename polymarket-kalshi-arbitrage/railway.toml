[build]
builder = "RAILPACK"

[deploy]
startCommand = "ARBITRAGE_CONFIG=config/settings.railway.yaml uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
